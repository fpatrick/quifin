// Main app entry page. It renders the full QuiFin shell.
import { AppShell } from "./components/app-shell";

/**
 * Renders the app shell as the home page.
 */
export default function Home() {
  return <AppShell />;
}
